### 1\. 悬浮输入窗 (The Quick Capture HUD)

这是**独立于主程序**的一个极简窗口（类似 Spotlight 或 Raycast）。

- **触发方式：** 全局快捷键（如 `Alt + Space`）。
- **界面包含：**
  - 一个输入框（文本/粘贴图片）。
  - 来源显示（例如：自动识别出“来自微信”）。
  - 快速标签（Priority, Due Date）。
- **定位：** **“无压力入口”**。用完即走，不打断当前工作。

---

### 2\. 主程序窗口 (The Main Application)

这个窗口通常采用 **侧边栏导航 (Sidebar Navigation)** 的布局。包含以下几个主要页面：

#### Page A: 智能看板 (Dashboard / Inbox)

“主页面”，但比单纯的 Upload + Todo 更丰富。

- **左侧/上方区域：** **智能待办列表**。不是简单的清单，而是根据我们之前设计的 `Priority Score` 排序的卡片。
- **右侧/下方区域：** **快速投递区 (Drop Zone)**。这里虽然支持手动上传（拖拽 PDF/图片），但视觉上不要做得太重。
- **主要交互：** 浏览今天要做什么。点击列表中的任务，跳转到 Page B。

#### Page B: 任务工作台 (Task Workspace)

任务页面。采用**分屏设计**，而不是单纯的平铺。

- **左半屏：** **执行区**。
  - AI 拆解的 Checklists。
  - 用户的笔记/输出区域。
  - Embedded Chatbox（针对该任务的 AI 助手）。
- **右半屏：** **上下文区 (Context)**。
  - 展示该任务关联的所有文件（PDF 预览、聊天记录片段）。
  - _亮点：_ 这里的 PDF 阅读器应该支持高亮，高亮的内容会自动变成左边的笔记。

#### Page C: 复盘与脉搏 (Review & Pulse) （优先级放后）

类似 ChatGPT 的 Pulse

- **界面：** 类似“股票走势图”或“健康报告”。
- **内容：**
  - “本周专注力分析”。
  - “历史上的今天”（AI 捞出的旧知识）。
  - 所有自动生成的日报/周报的时间轴归档。
  -

#### Page D: 知识宇宙 (Knowledge Universe) (优先级放后，锦上添花)

简历的“高光页面”。

- **界面：** 全屏的 WebGL Canvas。
- **交互：**
  - 拖拽旋转星系。
  - 点击节点（星球），弹出一个小卡片显示摘要。
  - 点击卡片上的“Open”，跳转回 Page B（查看详情）。

#### Page E: 设置 (Settings)

- **内容：**
  - OpenAI API Key 输入框。
  - 本地模型路径选择 (Ollama URL)。
  - 系统快捷键设置。

---

### 总结：页面层级图

```text
[系统层]
   └── 悬浮输入窗 (Quick Capture)

[主程序窗口]
   ├── 侧边栏 (Sidebar)
   │    ├── 📊 看板 (Dashboard) [Page A]
   │    ├── 📈 复盘 (Pulse)     [Page C]
   │    ├── 🔭 宇宙 (Universe)  [Page D]
   │    └── ⚙️ 设置 (Settings)  [Page E]
   │
   └── 主视图区域 (Main View)
        ├── (默认显示 Dashboard)
        └── (点击任务后跳转) -> 📝 工作台 (Workspace) [Page B]
```

**核心必须跑通的是：** 悬浮窗 -\> 看板 (Page A) -\> 工作台 (Page B)。

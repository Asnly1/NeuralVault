1. ✅ HUD 通过快捷键唤醒（Option + Space），界面模仿 Raycast
2. Dashboard 中的 QuickCapture 模块没有做好。我期望的是像 Gemini 网页端输入的那样。一个输入框，左下角有一个 "+"号可以上传文件，输入框剩下的就是空白
3. QuickCapture 不应该使用 CreateTask API，而是应该使用 quickCapture API
